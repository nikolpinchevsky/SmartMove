<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Horizontal arm (top) — 4dp thick -->
    <item android:bottom="24dp">
        <shape>
            <solid android:color="@color/scan_accent" />
            <corners android:radius="2dp" />
        </shape>
    </item>
    <!-- Vertical arm (left) — 4dp thick -->
    <item android:right="24dp">
        <shape>
            <solid android:color="@color/scan_accent" />
            <corners android:radius="2dp" />
        </shape>
    </item>
</layer-list>
